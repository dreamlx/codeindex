<!-- codeindex navigation index — agent: drill into source via Read/Grep for precise mechanism; do not treat this as final word. -->
<!-- Generated by codeindex (detailed) at 2026-05-26T10:09:00.078981 -->

# writers

## Overview

- **Files**: 6
- **Symbols**: 35

## Files

### __init__.py
_Smart README writer package with focused generator classes.

Provides level-specific README generation:
- OverviewGenerator: Project/root level docume_

### core.py
_SmartWriter facade and core types.

SmartWriter dispatches to level-specific generators and handles
file I/O, truncation, and backward-compatible dele_

**class** `class WriteResult`
> Result of writing a README file.

**class** `class SmartWriter`
> Smart README writer that generates appropriate content based on level.

    Levels:
    - overview: 

**Methods:**
- `def write_readme(
        self,
        dir_path: Path,
        parse_results: list[ParseResult],
        level: LevelType = "detailed",
        child_dirs: list[Path] | None = None,
        output_file: str = "README_AI.md",
    ) -> WriteResult`
- `def _generate_overview(
        self, dir_path: Path, parse_results: list[ParseResult], child_dirs: list[Path]
    ) -> str`
- `def _generate_navigation(
        self, dir_path: Path, parse_results: list[ParseResult], child_dirs: list[Path]
    ) -> str`
- `def _generate_detailed(
        self, dir_path: Path, parse_results: list[ParseResult], child_dirs: list[Path]
    ) -> str`
- `def _format_route_table(
        self, routes: list[RouteInfo], framework: str = "thinkphp"
    ) -> list[str]`
- `def _group_files(self, results: list[ParseResult]) -> dict[str, list[ParseResult]]`
- `def _filter_symbols(self, symbols: list[Symbol]) -> list[Symbol]`
- `def _get_key_symbols(self, symbols: list[Symbol]) -> list[Symbol]`
- `def _collect_recursive_stats(
        self, child_dirs: list[Path], output_file: str = "README_AI.md"
    ) -> dict`
- `def _collect_top_symbols(
        self, child_dirs: list[Path], output_file: str = "README_AI.md", limit: int = 15
    ) -> list[tuple[str, str, str]]`
- `def _extract_module_description(
        self, dir_path: Path, output_file: str = "README_AI.md"
    ) -> str`
- `def _extract_module_description_semantic(
        self,
        dir_path: Path,
        parse_result: Optional[ParseResult] = None
    ) -> str`
- `def _truncate_content(self, content: str, max_size: int) -> tuple[str, bool]`

_... and 1 more symbols_

### detailed_generator.py
_Detailed level README generator (leaf level)._

**class** `class DetailedGenerator`
> Generates detailed-level README content for leaf directories.

**Methods:**
- `def generate(
        self,
        dir_path: Path,
        parse_results: list[ParseResult],
        child_dirs: list[Path],
    ) -> str`
- `def _format_file_symbols(self, result: ParseResult) -> list[str]`

### navigation_generator.py
_Navigation level README generator (module level)._

**class** `class NavigationGenerator`
> Generates navigation-level README content for module directories.

**Methods:**
- `def generate(
        self,
        dir_path: Path,
        parse_results: list[ParseResult],
        child_dirs: list[Path],
    ) -> str`

### overview_generator.py
_Overview level README generator (root/project level)._

**class** `class OverviewGenerator`
> Generates overview-level README content for project/root directories.

**Methods:**
- `def generate(
        self,
        dir_path: Path,
        parse_results: list[ParseResult],
        child_dirs: list[Path],
    ) -> str`

### utils.py
_Shared pure utility functions for README generation.

These functions were extracted from SmartWriter's private methods.
They are stateless and take e_

**Functions:**
- `def collect_recursive_stats(
    child_dirs: list[Path], output_file: str = "README_AI.md"
) -> dict`
- `def extract_module_description(
    dir_path: Path, output_file: str = "README_AI.md"
) -> str`
- `def collect_top_symbols(
    child_dirs: list[Path],
    output_file: str = "README_AI.md",
    limit: int = 15,
) -> list[tuple[str, str, str]]`
- `def group_files(
    results: list[ParseResult], config: IndexingConfig
) -> dict[str, list[ParseResult]]`
- `def filter_symbols(
    symbols: list[Symbol], config: IndexingConfig
) -> list[Symbol]`
- `def format_route_table(
    routes: list[RouteInfo], framework: str = "thinkphp"
) -> list[str]`
- `def truncate_content(content: str, max_size: int) -> tuple[str, bool]`

## Dependencies

- ..adaptive_selector
- ..config
- ..framework_detect
- ..parser
- ..route_registry
- ..semantic_extractor
- .core
- .detailed_generator
- .navigation_generator
- .overview_generator
- .utils
- collections
- dataclasses
- pathlib
- re
- typing
